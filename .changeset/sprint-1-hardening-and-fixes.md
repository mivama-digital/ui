---
"@mivama/ui": minor
---

Hardening and component improvements (Sprint 1):

- Normalize Card and Sidebar Shell arbitrary property CSS variable syntax for Tailwind CSS v4 compatibility.
- Calibrate Button `sm` (36px) and `icon-sm` (36px) sizes for clear visual hierarchy against the 44px default.
- Enhance Toast system with `success` and `warning` variants, status icons, `toast.warning()` API, and safe standalone `Toaster` provider wrapping.
- Export `ToastVariant` type and update public API surface report.
- Reconnect Accordion open/close keyframe animations.
- Enhance FieldError with `role="status"` and `aria-live="polite"` for accessible form validation error announcements.
- Add Next-Iteration Plan documentation and maintainer guides.
