{
  "name": "mivama-shadcn-registry",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "shadcn build",
    "test": "node --test tests/registry-contract.test.mjs",
    "verify": "npm run build && npm run test && git diff --check"
  },
  "devDependencies": {
    "shadcn": "4.21.0"
  }
}
